version: '3.8'
services:
  mongo:
    image: mongo:6.0
    restart: unless-stopped
    ports:
      - '27017:27017'
    volumes:
      - mongo_data:/data/db

  backend:
    build: .
    env_file: .env
    ports:
      - '8000:8000'
    depends_on:
      - mongo

volumes:
  mongo_data: